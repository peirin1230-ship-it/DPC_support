# DPC検索ツール 検証報告書（2026年9月3日）

対象リポジトリ: `peirin1230-ship-it/DPC_support`（検証開始時点のコミット `bb14959`、修正反映後 `ef92601`）
対象データ: 令和8年度 DPC電子点数表 `database/001737745.xlsx`（有効期間の基準日 2026-08-13、3,267 DPC・504 分類）

本書は次の3つの問いに答えるために実施した検証の結果をまとめたものです。

1. このシステムは実証（現場での試行・評価）に耐えうるか
2. 医事課職員や現場の医師がコーディングをするうえで便利な機能はほかにないか
3. 現状の設計がベストなのか

---

## 0. 結論（要約）

| 問い | 結論 |
|---|---|
| 実証に耐えうるか | **条件付きで「耐えうる」**。点数表データと判定ロジックは一次資料（保医発0321第6号・コーディングテキスト第6版・電子点数表前提条件）と整合し、全3,267 DPCに対するプロパティテストで不一致はゼロ。検証で見つかった致命的・重大な不具合（サジェストで159分類が必ず0件、変換テーブル縮約の未反映、「下から優先」違反 など）は本検証の中で修正し、回帰テストを追加した。ただし現状は「候補を探す参照ツール」であり、「1患者1回の操作で確定DPCと根拠を出す」コーディング支援としては、複数入力の一括判定・副傷病自動判定・入院日ベースの期間計算・判定根拠の提示が無い。現行フローとの所要時間・誤り率比較を目的とする実証には、これら P0 機能の実装が前提になる。 |
| 便利な機能はほかにないか | 医事課・医師・設計の3視点から **合計 40 件超の提案**を整理し、優先度 P0〜P3 に格付けした（第7章）。P0 は「複数入力の一括判定」「定義副傷病の自動判定」「入院日・退院日入力と期間境界の暦日表示」「判定根拠パネル（摘要欄向けテキスト出力）」「DPCコード逆引き」。 |
| 現状の設計がベストか | **概ね妥当（設計批評の総合評価 4/5）**。維持すべきは「単一HTML配布（file:// でも動作）」「純関数のロジック層」「Excel→data.js の再生成一致検証とCI」。変えるべきは「基準日1点のスナップショットで年度・月次の切替ができない」「`D` シングルトンで複数データセット・テスト用データを差し替えられない」「DPCTool.jsx への状態集中」「UI・E2E テストの不在」。 |

### 本検証で修正した不具合（すべて master に反映済み）

| 重大度 | 件数 | 代表例 |
|---|---|---|
| critical | 1 | サジェストで手術分岐のない159分類（352 DPC）が必ず「0件のDPC候補」になる |
| high | 5 | 変換テーブルの縮約未反映（狭心症＋PCI＋心カテで一致なし）、処置＋薬剤同時入力で相関値が先勝ち、未入力側の分岐を「なし」に固定、7-8桁目（年齢・病態等）ステップ欠落、組み合わせ手術の要素単独一致 |
| medium | 13 | 管理料の97扱い、C97/T14 の無警告、出来高薬剤の無警告、係数注記、重症度判定時点の注記、副傷病注記、多区分Kコードの未解決、出来高DPCの自動確定隠れ、類似分類ジャンプ後の警告残留、全出来高分類でのサジェスト空白、お気に入りの非同期、エラー境界、フォント読み込みブロック ほか |
| low | 4 | 一覧カードの副傷病要約の手術スコープ、フィードバック文言、モーダル意味論、as-of の開始日判定 |

---

## 1. 検証の方法

### 1-1. 一次資料
- 保医発0321第6号「診断群分類点数表等の算定方法について」（PDF全文をテキスト化し、143 条のルールに分解）
- DPC/PDPS 傷病名コーディングテキスト 第6版（同上）
- 電子点数表「ＤＰＣ電子点数表の前提条件」「ダミーコード一覧」シート、令和6年度 樹形図・定義テーブル（分岐順序の機械検証に使用）

抽出した143条のうち、ツールに関係するものを `must_implement`（判定ロジックが従うべき規則）23条、`should_show`（利用者に提示すべき注記・警告）45条に分類し、照合対象とした。

### 1-2. 検証チーム（マルチエージェント）
| 担当 | 観点 | 主な手法 |
|---|---|---|
| G3 データ意味論 | 電子点数表19シートの意味と生成スクリプトの解釈 | Excel直読・樹形図2,982葉の順序検証・変換テーブル9,248行の突合 |
| G4 アーキテクチャ | コード構造・状態管理・a11y・性能・テスト範囲 | 静的解析、Playwright による実機再現、Node 計測 |
| A1 ルール準拠 | 68条の照合表作成、違反の再現 | utils 直接呼び出し＋ブラウザ再現（スクリーンショット付き） |
| A2 プロパティテスト | 全3,267 DPC・504分類の網羅検証（10項目） | サジェスト経路シミュレーション、calcTotal 140,675ケース、点数表行順の単調性 |
| A7 設計批評 | 現行設計の妥当性と代替案 | 配布性の実測（file://・外部遮断）、代替案比較 |
| A8 医事課視点 | 実務フロー（収集→決定→期間管理→請求→査定→委員会）との対応 | 機能提案 17件 |
| A9 医師視点 | 病名入力精度、傷病名選択ルールの注意喚起、説明性、教育用途 | searchDisease の実測、機能提案 |
| 統合審査 | 提案の重複排除と P0〜P3 の格付け | 効果×工数×規則適合 |

### 1-3. 動作検証環境
- Node 22、`node --test`（58テスト）、Vite 5 本番ビルド
- Playwright（headless Chromium）で `dist/index.html` を `file://` で開き、外部通信を遮断した状態でも検証

---

## 2. 総合評価（実証に耐えうるか）

| 領域 | 評価 | 根拠 |
|---|---|---|
| データ整合性 | ◎ | Excel から決定的に再生成でき、CI で一致を検証。3,267 DPC の日数・点数は点数表シートと完全一致。CCPM 支払分類内の点数一致、ダミーコード392件の名称存在、ICD 5,898件の分類解決を確認 |
| 判定ロジック（純関数層） | ◎（修正後） | ICD→分類 10,177クエリ・calcTotal 140,675ケース・分岐候補5,364項目・組み合わせ手術137件で不一致ゼロ。修正前は縮約未反映と「下から優先」違反があった |
| サジェスト（UI 経路） | ○（修正後） | 修正前は包括DPCの 264/2,449 件が到達不能（うち256件はラベル不一致バグ）。修正後は 7-8桁目ステップの追加と出来高行の表示により経路が閉じない |
| ルール準拠（must_implement 23条） | ○ | 修正前 17適合/6部分 → 修正後 21適合/2部分（残: 一覧モードの「DPC対象外」区別、Ⅲ超後の手術注記） |
| 利用者への注記（should_show 45条） | △ | 修正前 4部分/41未表示 → 修正後 17適合・部分/28未表示。用語定義・入院日数の起算・分類固有の注意・留意ICD が未表示（第6章 残課題） |
| 性能 | ◎ | 初回表示 0.16〜0.24秒（フォント非ブロック化後）、最頻処置コード検索 3.6ms/1,082件、分岐候補生成 10.5ms、dist 1.0MB（gzip 301KB） |
| アクセシビリティ | ○ | skip link・focus-visible・combobox/listbox・フォーカストラップ・reduced-motion は実装済み。モーダル意味論とキーボード操作を本検証で補完。9〜10px・低コントラスト文字（#8B8B8B, #A3A3A3）が残る |
| セキュリティ・依存 | ○ | 外部通信は Google Fonts のみ（非ブロック化済み、未取得時はシステムフォント）。npm audit は開発時依存のみ 3件残（vite/esbuild は Vite 8 への major 更新が必要、xlsx は修正版なし・厚労省ファイルの生成時読込のみに使用） |
| 保守性 | △ | 生成スクリプトが列位置リテラル依存、DPCTool.jsx 800行に状態34個、UI テスト無し |

**判定**: 「参照ツール」として院内試行に出せる品質。ただし、実証で「コーディング所要時間・誤り率が現行フローより改善するか」を測るなら、第7章の P0 機能（1患者1回で確定DPCと根拠を出す機能群）の実装後に行うべきである。

---

## 3. ルール準拠性の照合結果

### 3-1. must_implement（判定ロジックが従うべき規則）23条

| 規則 | 内容（要約） | 検証前 | 検証後 | 備考 |
|---|---|---|---|---|
| CT-03/05/08 | 14桁構造・桁の意味 | 適合 | 適合 | |
| CT-06 | 97 の意味（分岐が「あり/なし」のみの分類では「手術あり」） | 適合 | 適合 | 178分類「あり」、167分類「その他の手術あり」 |
| CT-07 | 11・12桁目の値（2以上は定義項目あり、A〜G） | 部分 | **適合** | 変換テーブルの縮約（対応コード1,2→桁0 等）を `D.cv` として生成し、判定前に桁へ変換 |
| CT-10 | R/Z/U コードの選択不可 | 部分 | 部分 | サジェストのみ「DPC対象外」表示。一覧モードの文言区別は残課題 |
| CT-22 | 産科DIC（O081/O723/O450→120290） | 適合 | 適合 | |
| CT-24 | Rコード例外9件 | 適合 | 適合 | データ上 R040/R042/R048/R049/R560/R61$/R730 のみ収載 |
| CT-50 | Mコード部位桁・M!!!! | 適合 | 適合 | |
| CT-60 | MDC一覧 | 適合 | 適合 | |
| CT-64 / NT-15 | 下から優先（全項目） | 部分 | **適合** | 処置＋薬剤の相関値最大採用、未入力側の非固定、多区分Kコードの区分決定規則を明示化 |
| CT-65 | 手術の定義（管理料除外・＋・準用） | 部分 | 部分 | 管理料→99、組み合わせは全要素一致、準用注記を実装。「Ⅲ超後の手術もあり」注記は未 |
| CT-67 / NT-19 | 副傷病フラグ 1/2/3 | 適合 | 適合 | 一覧カードの要約も手術スコープを反映するよう修正 |
| CT-70 / NT-18 / NT-47 | 期間Ⅰ〜Ⅲ・Ⅲ超の出来高 | 適合 | 適合 | 期間Ⅱ「-」の分類（d2=d1）も処理 |
| CT-73 / NT-09 | 出来高分類（点線）の表示 | 適合 | 適合 | サジェストでも出来高行を「（出来高算定）」付きで選択肢に表示するよう修正 |
| NT-04 | 臓器移植等の出来高手術 | 適合 | 適合 | D.dk 74件 |
| NT-11 | 選択禁止ICD（B89/B95-99/R） | 適合 | 適合 | |
| NT-23 | 組み合わせ手術は全実施時のみ | 部分 | **適合** | 要素単独では一致させず、ヒント表示 |

### 3-2. should_show（利用者に提示すべき注記・警告）45条

| 区分 | 規則 | 検証後 |
|---|---|---|
| **実装済み** | NT-12/CT-27/CT-72（C97・T14 選択不可、T08/T10/T12 留意）、NT-16（準用手術）、NT-20（副傷病は併存症＋入院後発症、疑い除く）、NT-21/22/32/34/35/36/39/40/CT-68/CT-69（JCS・年齢の判定時点、膵炎不明→軽症、発症時期、A-DROP、妊娠週数、出血量、Child-Pugh）、NT-41（係数を含まない注記）、NT-06（出来高薬剤の可能性バッジ）、CT-65 内の管理料注記 | 17条 |
| **未表示（残課題）** | CT-09（手術から病名を遡らない）、CT-15/CT-41/CT-58（修飾語・詳細不明・留意ICD 17件）、CT-19（乖離時の症状詳記）、CT-20/21/23/30/45/59/NT-61（精緻な病名選択、DIC・敗血症・180040・AMI 28日、摘要欄記載）、CT-66/NT-24/26/58（化学療法・電気生理学的検査・退院時処方の定義）、NT-02/NT-08/NT-44（24時間以内死亡、摘要欄へのDPC併記、包括除外費用の一覧）、NT-48〜52/56/57/64（Ⅲ超後の薬剤・手術、外泊、7日以内再入院、地域包括ケア転棟・転室、転棟起算） | 28条 |

未表示の28条はいずれも「ツールが誤ったDPCを返す」類ではなく「利用者が通知を別途参照する必要がある」類であり、第7章の P1「用語定義・入院日数ヘルプ・分類固有注記」でまとめて解消できる。

---

## 4. データ整合性（電子点数表の解釈）

| 項目 | 結果 |
|---|---|
| シート構成 | 19シート。変更区分（0/1/2/3/5/9）・有効期間・更新日の解釈は前提条件シート 9〜10 行と一致。R8 ファイルでは変更区分はほぼ 0、終了日は全行 99999999 で、更新は「更新日」列にのみ現れる（生成スクリプトは開始日・更新日の最大値 2026-08-13 を基準日として正しく導出） |
| 樹形図の順序 | R6 樹形図 482分類・2,982葉を機械検証: 手術は上→下で 99→97→03→02→01（降順）、処置等1・2・副傷病・重症度は 0→1→…→B（昇順）。「下に掲げるDPCを優先」＝手術は番号が小さいほど優先、処置等は相関値が大きいほど優先。utils.js の優先ロジックはこれと整合 |
| 変換テーブル | 9,248行・3,267コード。包括フラグ以外に「対応コード→14桁コード」の写像を持ち、MDC05 の 9分類・16組で対応コード≠桁（縮約）。**検証前は未反映で、狭心症＋PCI＋心臓カテーテル法で「一致なし」となる実害を確認 → `D.cv` を生成して修正** |
| 97 の明示Kコード | 3,982行・308分類で 97 に KKK1 以外の明示Kコードが含まれる。D.si/D.sl は同一リストで保持し、定義外Kコードは 97 にフォールバック（前提条件29行と一致） |
| 副傷病フラグ | 1:583 / 2:283 / 3:510 行。D.sd に a/n/s スコープとして保持 |
| 重症度・年齢等 | 変換テーブルで「a」以外の値を持つ分類集合（病態等 040080、年齢等 12分類、重症度等 14分類）と D.pt/D.sv のキー集合が完全一致 |
| CCPM対応 | 010060（101コード）・040080（52コード）の全DPCが支払分類へ写像。同一 CCPM 内の日数・点数は完全一致（不一致0） |
| ダミーコード | 392件。D.p1/D.p2/D.sl で使う4桁・ダミーコードはすべて一覧に存在し名称あり。未使用41件は旧定義 |
| 選択不可ICD | C97、T140〜T149 が ICD シートに収載されており検索可能 → 警告を実装（削除はしない: 電子点数表の収載事実を保つ） |
| as-of | 検証前は開始日 > 基準日の行を除外していなかった → 除外するよう修正（`--as-of` で過去時点を再現可能） |

---

## 5. 動作検証（プロパティテスト・ブラウザ・性能）

### 5-1. プロパティテスト（全DPC網羅、A2）
| 項目 | 規模 | 結果 |
|---|---|---|
| ICD→分類（`$` 前方一致含む） | 10,177 クエリ | 漏れ0・複数分類0・重複0 |
| `$` 前方一致の衝突 | 1,070 プレフィックス | 衝突0 |
| calcTotal（日別積み上げとの一致） | 2,449 包括DPC × 入院日数 = 140,675 ケース | 不一致0 |
| 点数表行順と相関値タプルの単調性 | 1,221 グループ・2,046 隣接ペア | 違反0 |
| 手術ブロックの順序（99→97→降順） | 522 ブロック | 違反0 |
| 分岐候補（getBranchOptions） | 1,172 (分類,手術) × 5,364 項目 | 幽霊項目0・漏れ0 |
| ドリルダウンと展開結果の整合 | 5,607 ケース | 差分はすべて「未入力側=なし固定」で説明可 → 修正 |
| 組み合わせ手術 "K1+K2" 指定検索 | 137 件 | 全件正しい区分 |
| サジェスト経路の到達性 | 3,267 DPC | **修正前**: 到達不能 1,060（包括 264、うちラベル不一致 256・重症度ステップ 8）→ **修正後**: 原因2件を解消 |
| コード形式・ラベル網羅 | 3,267 | 欠落0 |

### 5-2. ブラウザ再現（Playwright、修正後に再確認）
- 低血糖症 E160（手術分岐なし）→ サジェストが候補を提示（修正前は 0件）
- 肺炎 J13 → 「年齢・病態等」ステップが表示され 1歳未満/15歳未満 の小児コードへ到達
- 狭心症 I200 ＋ K5493 ＋ D2061 → 050050 の DPC が一致（修正前は一致なし）
- 出来高手術 K1541 で検索後、類似分類へジャンプ → 警告と MDC 絞込がクリアされる
- お気に入り削除 → 一覧の★が即時更新、履歴パネルは Esc・外側クリック・ボタン再押下で閉じる
- フィードバック・履歴パネルが role=dialog でフォーカスが移動し Esc で閉じる、タブは矢印キーで切替
- 全DPCが出来高の分類（D358 → 100010）でサジェスト右パネルが空白にならない（修正前は空白）
- 初回表示: 外部通信遮断下で 0.16秒（修正前はフォント CDN 待ちで最大 12.7秒）

### 5-3. 性能（Node 22 / 単一実行）
| 処理 | 時間 |
|---|---|
| searchDPC J0451（1,082件） | 3.6 ms |
| getExpandedResults（1,082行） | 2.4 ms |
| getBranchOptions（1,082件） | 10.5 ms |
| searchDisease「はい」 | 11.2 ms |
| searchSurg「K5」 | 1.9 ms |
| dist/index.html | 1,014 KB（gzip 301 KB） |

### 5-4. セキュリティ・依存
- 外部リソースは Google Fonts のみ。fetch/XHR・外部スクリプト無し。データはすべて同梱
- localStorage: 検索履歴・お気に入り・フィードバック（個人情報の入力欄は無い）。フィードバックは端末保存のみである旨を画面文言で明確化
- `npm audit`: 9件 → 非破壊の `npm audit fix` で 3件（すべて開発時依存）。vite/esbuild は Vite 8 への major 更新、xlsx は上流修正なし（厚労省公式ファイルの生成時読み込み専用）

---

## 6. 発見した問題と対応

### 6-1. 修正済み（コミット `6d8b37b` / `7a966e2` / `aab179a` / `ef92601`）

| ID | 重大度 | 内容 | 対応 |
|---|---|---|---|
| A2-01 | critical | 手術分岐なし159分類でサジェストが必ず0件（ラベル文字列でキー照合） | `surgKey()` で値ベースのキーに統一。全分類での整合テストを追加 |
| A1-03 / G3-01 / A2-07 | high | 変換テーブルの縮約（MDC05 9分類）未反映 → 狭心症＋PCI＋心カテで一致なし | `D.cv` を生成、`corrToDigit()` で判定前に桁へ変換。データ不変条件テスト追加 |
| A1-02 | high | 処置＋薬剤の同時入力で相関値が先勝ち | 同一分岐は相関値最大を採用 |
| A1-01 / A2-06 | high | 未入力側の処置等分岐を「なし」に固定し、ドリルダウンで追加不可 | 未入力側は未制約に |
| A2-02 | high | サジェストに 7-8桁目（年齢・病態等）ステップが無く小児肺炎に到達不能 | 「条件」ステップを追加 |
| A2-04 / G3-07 / A1-16 | high→ | 組み合わせ手術の要素単独で組み合わせ区分に確定 | 完全一致のみ採用、要素入力時は「○○も実施なら区分xx」のヒント |
| A2-05 / A2-08 | medium | 多区分Kコード（24件）で複数区分が残る／キー順依存 | 区分決定規則を明示（単独一致優先 → 区分番号最小） |
| A1-04 | medium | 手術等管理料・輸血管理料（K914〜917, K920-2）が 97 扱い | 99 として検索し通知 第2の3(8) を注記 |
| A1-06 / G3-08 | medium | C97・T14 が無警告 | 選択不可・留意の警告（候補バッジ＋alert） |
| A1-07 | medium | 出来高算定薬剤（52件が処置等2と名称重複）の無警告 | `isDekidakaDrug()` による「出来高薬剤の可能性」バッジ |
| A1-08 | medium | 総点数が係数前の素点である注記なし | 全表示箇所に注記 |
| A1-09 | medium | 重症度・年齢等の判定時点・不明時の扱い未表示 | `severityNote()`/`conditionNote()`（JCS・膵炎・発症時期・A-DROP・Child-Pugh・年齢・妊娠週数・出血量） |
| A1-10 / G4-10 | medium | 副傷病の範囲注記なし、一覧カードの副傷病要約が手術スコープ無視 | 注記追加、`getSubdiagICDs(cls, sd, surg)` |
| A1-05 | medium | 97フォールバックに準用手術の注記なし | 一覧・サジェスト両方に注記 |
| A2-03 / G4-05 | medium | 出来高行を選択肢から除外し自動確定するため出来高患者に包括DPCが提示／全出来高分類で右パネル空白 | 出来高行を「（出来高算定）」付きで選択肢に含める |
| G4-04 | medium | 検索実行が4箇所に重複し、類似分類ジャンプ後に出来高警告・MDC絞込が残留 | `runSearch()` に集約し関連状態を同時初期化 |
| G4-06 | low | 履歴パネルでお気に入り削除しても★が残る | 親の state と同期 |
| G4-07 | medium | フィードバックが端末保存のみなのに「送信しました」 | 文言を「このブラウザに保存しました／エクスポートして提出」に |
| G4-08 | medium | エラー境界なし（描画例外で白画面） | ルート ErrorBoundary（再読み込み・保存データ消去） |
| G4-11 | medium | フィードバック・履歴に dialog 意味論なし、タブの矢印キー非対応 | useModal 適用、outside click、ArrowLeft/Right |
| G4-13 / A7-07 | medium | Google Fonts の `<link>` が初回描画をブロック（CDN 不達で約12.5秒） | preload＋onload で非ブロック化 |
| G4-14 | low | 結果ソートが毎レンダー実行 | useMemo |
| G3-03 | medium | `--as-of` が開始日を判定しない | 開始日 > 基準日の行を除外 |
| 依存 | - | npm audit 9件 | 非破壊修正で 3件（開発時依存のみ）に |

### 6-2. 残課題（未修正）

| ID | 重大度 | 内容 | 推奨対応 | 工数 |
|---|---|---|---|---|
| A1-11 | medium | 化学療法・放射線療法・電気生理学的検査・退院時処方などの用語定義が未表示 | ダミーコード／分類キーの静的注記テーブル | S |
| A1-12 | medium | 入院日数の起算・通算（外泊、7日以内再入院、転棟、地域包括ケア、Ⅲ超後の薬剤・手術）の注記なし | 入院日数欄のヘルプ、Ⅲ超時の赤字注記 | M |
| A1-13 | medium | アップコーディング注意分類（130100/180010/180040/120290）・AMI 28日・摘要欄記載義務が未表示 | 分類→注記テーブル、「要注意」バッジ | M |
| A1-14 | low | 留意ICD 17件・詳細不明 644件の区別なし | 候補バッジ＋細分類コード提示 | S |
| A1-15 | low | 出来高一覧の収載範囲（電子点数表分のみ）・包括除外一般則・24時間以内死亡・摘要欄併記の注記なし | パネル冒頭の説明と静的タブ | S |
| A1-17 / CT-10 | low | 一覧モードで「DPC対象外」と「未収載」を区別しない | findCls 空時のメッセージ分岐 | S |
| CT-65 一部 | low | 「Ⅲ超後の手術も手術あり」の注記 | 入院日数ヘルプに含める | S |
| G3-02 / G3-04 / G3-09 | low | 変換テーブル使用列と D.pt/D.sv の突合、未使用列の非空検知、CCPM 点数一致を生成時の検証に追加 | generate-data の検証段に追加 | S |
| G3-10 | low | 出来高算定コードの区分（臓器移植/大臣指定）を保持しない | D.dk を [名称, 区分] に | S |
| G3-11 | low | 040080「市中肺炎以外」の暗黙条件（15歳以上）がラベルに無い | 補助ラベル併記 | S |
| G4-09 | medium | 表示・絞込がラベル文字列（"なし"/"-"）に依存 | 値ベースの真偽値を結果に持たせる | S |
| G4-11 残 | medium | 9〜10px・低コントラスト文字（#8B8B8B 3.4:1、#A3A3A3 2.5:1）、サブタブの role=tab 無し | 最小 11px／#737373 以上に統一 | M |
| G4-03 / A7-05 | medium | DPCTool.jsx に状態34個・props 20個 | useReducer＋Context へ | L |
| G4-15 / A7-03 | medium | 表示層とロジック層が `D` の内部構造に直接結合 | `createEngine(D)` とアクセサ | L |
| G4-17 / A7-11 | medium | UI・E2E・全DPC到達性テストが無い | prop.mjs の取り込み、vitest+RTL、Playwright スモークを CI に | M |
| G4-18 | low | 生成スクリプトが列位置リテラル依存・非関数化・D.dk の自己参照フォールバック | 見出し名→列マップ、関数化、フォールバックは明示フラグ時のみ | M |
| G4-12 / A7-06 | low | 全インライン style、印刷スタイル無し | CSS Modules＋トークン、@media print | M |
| G4-19 / A7-08 | low | localStorage にスキーマ版なし、data.js が1行で diff 不可、ビルド識別子なし | 封筒 {v, items}、整形JSON出力、`__BUILD_COMMIT__` 表示 | S |
| A7-01 / A8-12 | high（実運用） | 基準日1点のスナップショットで退院日に応じた年度・月次切替ができない | 行単位の有効期間保持＋退院日フィルタ、年度別データ同梱 | L |
| A7-13 | medium | 月次更新の検知が完全手動 | 週次 Actions で厚労省ページを照合し PR 自動作成 | M |

---
