{
  "name": "dpc-tool",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "engines": {
    "node": ">=20.11"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "generate:data": "node scripts/generate-data.mjs",
    "validate:data": "node scripts/validate-dpc-data.mjs",
    "test": "node --test tests/data.test.mjs tests/utils.test.mjs",
    "validate:all": "npm run validate:data && npm test && npm run build"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.14",
    "vite-plugin-singlefile": "^2.0.3",
    "xlsx": "^0.18.5"
  }
}
